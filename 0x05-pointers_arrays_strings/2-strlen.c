#include "main.h"
#include <stdio.h>
/**
 * _strlen  - char as input
 * @s :char variable
 * Return: Always 0.
 */

int _strlen(char *s)
{
int i = 0;
for (i = 0; s[i] != '\0'; ++i);
{
i = i;
}
return (i);
}
