#include "main.h"
#include <stdio.h>
/**
 * main - check the code.
 *
 * Return: Always 0.
 */

int _isupper(int  c)
/**
 * main - check the code.
 *
 * Return: always 0.
 */
{
if (c <= 'A' && c >= 'A')
{
return (1);
}
else
{
return (0);
}
}
