#include "main.h"
#include <stdio.h>
/**
 * _isupper - check the code.
 * @c: character as input
 * Return: Always 0.
 */
int _isupper(int  c)
{
  if (c <= 'A') && (c >= 'A')
{
return (1);
}
else
{
return (0);
}
}
