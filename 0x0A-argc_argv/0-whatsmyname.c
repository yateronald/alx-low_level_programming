#include "main.h"
/**
 * main - prints its name + \n
 * @argc: count number of argument
 * @argv: olding all the
 * Return: 0
 */

int main(int argc, char *argv[])
{
printf("%s", argv[0]);
printf("\n");
return (0);
}
